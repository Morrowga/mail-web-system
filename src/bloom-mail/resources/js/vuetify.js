import { createVuetify } from 'vuetify';
import 'vuetify/styles'; // Include the Vuetify styles
import { createApp } from 'vue';

const vuetify = createVuetify(); // Create a Vuetify instance

export default vuetify;
